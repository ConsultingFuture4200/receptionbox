---
phase: 03-cloud-derate
plan: 06
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04", "03-05"]
files_modified:
  - gates/g1/runner.py
  - tools/run_phase3_gate.py
  - results/g1/
autonomous: false
budget_usd: 12.0
requirements:
  - GATE-G1
user_setup:
  - service: runpod
    why: "Provision v18 H100 pod for G1 500-call latency corpus at N=1/2/4 concurrencies via full LiveKit pipeline"
    env_vars:
      - name: RUNPOD_API_KEY
        source: "Operator env"
    dashboard_config: []
must_haves:
  truths:
    - "G1 latency measured on the full 500-call corpus at three concurrency levels (N=1, N=2, N=4) on a single H100 pod session = 1500 rows total"
    - "Each row carries per-stage timings: stt_ttft_ms, llm_ttft_ms, llm_decode_ms_per_tok, tts_first_audio_ms, e2e_ms (all five fields populated per Phase 02 GateResult schema)"
    - "Per-row concurrency level recorded; bootstrap CIs can be computed per (stage × concurrency) bucket in Plan 03-07"
    - "Spend ≤ $13 against $12 budget envelope (matches CLAUDE.md §13 G1 line item)"
    - "This is the LAST cloud spend of Phase 3; cheaper gates 03-02..05 already produced results, so any G1-specific regression is isolatable to the LiveKit pipeline (substrate proven healthy in 03-01 + 03-02)"
  artifacts:
    - path: "results/g1/{run_id}.jsonl"
      provides: "1500 rows (500 calls × 3 concurrency levels) with per-stage timings"
      contains: "\"concurrency\":"
    - path: "results/preflight/03-06-session-{ts}.json"
      provides: "Session manifest with per-concurrency row counts + median e2e_ms + true spend"
      contains: "concurrency"
  key_links:
    - from: "gates/g1/runner.py"
      to: "GateRunner.__init__(concurrency=N)"
      via: "concurrency level passed at runner construction; influences asyncio.Semaphore in run_all"
      pattern: "concurrency"
    - from: "tools/run_phase3_gate.py"
      to: "subprocess ssh exec running g1 runner 3 times with --concurrency=1, 2, 4"
      via: "loop in driver: for c in [1,2,4]: exec(python -m gates.g1.runner --concurrency=c)"
      pattern: "concurrency"
---

<objective>
Run G1 latency measurement on the full 500-call corpus at concurrencies N=1, N=2, N=4 via the LiveKit pipeline rig on a single H100 pod session. This is the MOST EXPENSIVE plan in Phase 3 (~$12 vs $3-5 for others); it runs LAST after cheaper plans have validated the substrate so any G1-specific issue is isolated to the LiveKit pipeline orchestration, not the substrate (which 03-01..05 already proved healthy).

Purpose: G1 is the load-bearing measurement for the synthesis report's per-stage Orin derate (CLAUDE.md §7.2 prescribes per-stage derate logic; G1 supplies the per-stage source measurements). The 1500-row matrix (500 calls × 3 concurrencies) lets Plan 03-07 compute bootstrap 95% CIs per stage AND quantify concurrency scaling — a key input for the Phase 4 sizing question (how many concurrent calls can one Orin serve?).

Output:
- `results/g1/{run_id}.jsonl` with 1500 rows.
- Session manifest at `results/preflight/03-06-session-{ts}.json`.
- A `g1_summary` block in manifest: `{n_total, n_ok, n_err, per_concurrency: {1: {n, median_e2e_ms}, 2: {...}, 4: {...}}, true_spend_usd}`.

Per CONTEXT.md §specifics fail-cheap-first ordering: this plan MUST execute AFTER all of 03-02/03/04/05 have completed with their checkpoints approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-cloud-derate/03-CONTEXT.md
@./CLAUDE.md
@gates/g1/runner.py
@gates/_runner_base.py
@substrate/cuda.py
@substrate/livekit_pipeline.py
@tools/run_phase3_gate.py
@assets/corpus_500/

<interfaces>
From gates/g1/runner.py (Phase 02-validated; smoke session 20260509T231720Z):
```python
class G1Runner(GateRunner):
    async def run_one(self, asset: dict) -> GateResult:
        # Drives one call through the LiveKit AgentSession pipeline
        # Captures stt_ttft_ms, llm_ttft_ms, llm_decode_ms_per_tok,
        # tts_first_audio_ms, e2e_ms via per-stage timestamps emitted by
        # AgentSession's event callbacks.
```

Concurrency is currently a Phase 02 CLI arg (`--concurrency=N` defaults to 1). G1Runner is already built atop the asyncio.Semaphore pattern in `GateRunner.run_all` (line 194 of `_runner_base.py`); changing concurrency = changing one constructor arg. NO new runner code needed; Plan 03-06 only invokes G1Runner 3 times with different `concurrency` values.

From assets/corpus_500/: 500 stratified call recordings + metadata; Phase 1-curated, manifest-pinned.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add concurrency sweep mode to tools/run_phase3_gate.py</name>
  <files>tools/run_phase3_gate.py, tests/test_run_phase3_gate.py</files>
  <read_first>
    - tools/run_phase3_gate.py (Plan 03-02 driver; per-gate exec function)
    - gates/g1/runner.py (concurrency arg)
  </read_first>
  <action>
    Extend `tools/run_phase3_gate.py` to support `--concurrency-sweep` flag (`--concurrency-sweep 1,2,4`). When set:

    1. The driver loops over the concurrency list, invoking the gate runner once per concurrency level (sequentially on the same pod session — not parallel; the H100 throughput is finite and concurrency is the measurement axis, not a parallelization knob for the meta-loop).
    2. Each sub-invocation passes `--concurrency=N` to the gate runner.
    3. Each sub-invocation's exit code + row count is recorded under `manifest["gate_results"][gate]["concurrency_runs"][N] = {exit_code, row_count, run_id}`.
    4. The pod stays UP for the entire sweep (one provision, one terminate; 3 sub-runs in between).
    5. `--concurrency-sweep` defaults to "1" (single run) when not specified — backward compatible with plans 03-02/03/04/05.

    Add `tests/test_run_phase3_gate.py` test extending the existing suite (Test 6) — `--gate=g1 --concurrency-sweep=1,2,4 --dry-run` returns 0 and manifest shows 3 sub-runs planned for g1.

    Acceptance: `uv run pytest tests/test_run_phase3_gate.py -x -q` clean; `uv run ruff check tools/run_phase3_gate.py` clean.
  </action>
  <verify>
    <automated>uv run pytest tests/test_run_phase3_gate.py -x -q &amp;&amp; grep -q "concurrency-sweep" tools/run_phase3_gate.py</automated>
  </verify>
  <done>
    Driver supports concurrency sweep on one pod session; new test passes; existing tests still pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator real-spend run — G1 at N=1/2/4 on fresh v18 pod</name>
  <what-built>
    `tools/run_phase3_gate.py --concurrency-sweep=1,2,4` runs G1Runner three times on one pod session with concurrency varying.
  </what-built>
  <how-to-verify>
    Operator runs (this is the LARGEST single spend in Phase 3):

    ```bash
    cd ~/RBOX
    uv run python -m tools.run_phase3_gate \
        --gate=g1 \
        --concurrency-sweep=1,2,4 \
        --real-spend --max-minutes=240
    ```

    Wait ~3-4 hours. Expected:
    1. `results/g1/*.jsonl` contains 1500 rows total across 3 jsonl files (one per concurrency).
    2. Each row has all five per-stage fields populated: `stt_ttft_ms`, `llm_ttft_ms`, `llm_decode_ms_per_tok`, `tts_first_audio_ms`, `e2e_ms`.
    3. Each row has `concurrency` field ∈ {1, 2, 4}.
    4. Session manifest's `g1_summary.per_concurrency` shows row counts ~500 per level (allow ±5% for errors).
    5. Median `e2e_ms` increases monotonically with concurrency (sanity: more concurrent calls = higher queueing latency).
    6. Spend ≤ $13.
    7. Pod GONE.

    Operator types:
    - `approved` if 1-7 hold (this UNBLOCKS Plan 03-07 synthesis).
    - `fail: {reason}` otherwise.

    NOTE: If spend trends > $11 with concurrency=4 not yet started, operator may abort via Ctrl-C; partial results (N=1 + N=2) are still pulled back and Plan 03-07 can run a partial-coverage derate flagged as such.
  </how-to-verify>
  <resume-signal>Type "approved" or "fail: {reason}" or "partial: {N values completed}"</resume-signal>
</task>

</tasks>

<verification>
- `tools/run_phase3_gate.py` supports `--concurrency-sweep`.
- Operator G1 real-spend run produces 1500 rows on a single pod session.
- All 5 per-stage timing fields populated on every row.
- Spend ≤ $13 against $12 envelope.
- Cumulative Phase 3 spend (sum across 03-01..06) ≤ $35 against ~$49 ceiling.
</verification>

<success_criteria>
G1's full 500-call × 3-concurrency matrix lands; Plan 03-07 has all the per-stage source measurements needed to derate to Orin and compute bootstrap CIs per (stage × concurrency). This is the deepest measurement in Phase 3 and the principal input for Phase 4's go/no-go evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/03-cloud-derate/03-06-SUMMARY.md` with: pod_id, jsonl paths + row counts per concurrency, median per-stage timings per concurrency, true spend, cumulative-Phase-3 spend, and any concurrency-related anomalies.
</output>
