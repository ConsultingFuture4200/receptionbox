---
plan: 03-06
type: prestage-notes
written: 2026-05-12
status: review-only (no code changes)
---

# Plan 03-06 — Pre-stage readiness notes

Written ahead of W3 firing so the operator can hit the ground running.
This is a read-only inventory: code is **not** modified here.

## Readiness inventory (vs. plan assumptions)

| Plan assumes | At HEAD (`6414695`) | Status |
|---|---|---|
| `tools/run_phase3_gate.py` exists (from 03-02 Task 2) | **Absent** — `ls tools/` shows only `find_runpod_volume.py`, `poll_runpod_mi300x.py`, `probe_runpod_dc.py`, `probe_runpod_stock.py` | **BLOCKING** |
| 03-02 complete (driver + budget cap + dry-run test) | `03-02-SUMMARY.md` not written; ROADMAP shows 03-02 unchecked; only G3 sweep + strata.yaml + v19a digest pin committed | **BLOCKING** |
| `gates/g1/runner.py` CLI accepts `--concurrency=N` | CLI parser at lines 123–143 has no `--concurrency` arg (`--gate`, `--n-calls`, `--strata`, `--corpus`, `--vllm-url`, `--vllm-model`, `--whisper-dir`, `--chatterbox-url`, `--kokoro-url`, `--results-dir` only) | **SMALL GAP** |
| `GateRunner` accepts `concurrency` kwarg + uses `asyncio.Semaphore` in `run_all` | `gates/_runner_base.py:100` (`concurrency: int = 1`), `:194` (`sem = asyncio.Semaphore(self.concurrency)`) — both present | **READY** |
| `assets/corpus_500/` has 500 stratified calls + manifest entries | `ls assets/corpus_500/ | wc -l` = 500; `grep -c corpus_500 assets/manifest.csv` = 500 | **READY** |
| Image v19a pinned in `orchestration/runpod_h100.py` lockfile | Pinned in commit `254b715` | **READY** |
| `phase3_strata.yaml` + `STRATA_PATH` env override available | Committed in `cf55e95` | **READY** |
| Wave gating: depends_on 03-02 / 03-03 / 03-04 / 03-05 | None checkpointed yet (03-01 is the only Phase 3 plan with SUMMARY) | **GATED** |

## What still has to land before 03-06 can fire

1. **03-02 Task 2 — build `tools/run_phase3_gate.py`** (the unified Phase 3 gate driver). Plan 03-06 Task 1 amends this driver with `--concurrency-sweep`; the file has to exist first.
2. **G1 runner CLI gains `--concurrency` flag** — a ~10-line patch in `gates/g1/runner.py::_build_arg_parser()` + a passthrough into the `G1Runner(..., concurrency=args.concurrency)` constructor. The plumbing in `_runner_base.py` is already complete; the CLI hole is the only gap. Should be folded into 03-02 Task 2 or filed as a 03-02 follow-up before 03-06 fires.
3. **03-03 / 03-04 / 03-05 complete + checkpoint-approved** — per plan dependency declaration and CONTEXT.md fail-cheap-first ordering.

## Cost/time envelope reminders

- Budget: $12 against $13 hard cap (CLAUDE.md §13 G1 line item; matches plan header)
- Wall-clock: ~3-4 h for the full 500×3 sweep on one H100 pod session
- This is the **last** Phase 3 cloud spend; any G1-specific regression is isolatable to the LiveKit pipeline (substrate proved healthy in 03-01 + 03-02)
- Cumulative Phase 3 spend ceiling: ≤ $35 against ~$49 envelope after this run

## Operator command (validated paths/args, for the W3 checkpoint)

```bash
cd ~/code/RBOX   # NOTE: repo moved from ~/RBOX to ~/code/RBOX (venv shebangs
                 # are re-bound after `uv sync`; pre-commit hook path patched
                 # in commit 6414695-followup)
uv run python -m tools.run_phase3_gate \
    --gate=g1 \
    --concurrency-sweep=1,2,4 \
    --real-spend --max-minutes=240
```

Expected on success (verbatim from plan §how-to-verify):
1. `results/g1/*.jsonl` contains 1500 rows across 3 files (one per concurrency).
2. Each row has all five per-stage timing fields populated.
3. Each row has `concurrency` ∈ {1, 2, 4}.
4. Session manifest's `g1_summary.per_concurrency` shows ~500 rows per level (±5%).
5. Median `e2e_ms` is monotonically non-decreasing in concurrency.
6. Spend ≤ $13.
7. Pod GONE.

Partial-coverage rule: if spend trends > $11 with concurrency=4 not yet started, operator may Ctrl-C; N=1 + N=2 results still flow into Plan 03-07 as a partial-coverage derate (flagged as such).

## Environment notes (carried forward to W3)

- Repo moved `/home/bob/RBOX → /home/bob/code/RBOX` since the venv was first built. `uv sync` rebuilds the venv with correct shebangs. `.git/hooks/pre-commit` is generated by `pre-commit install` and hard-codes the python path — needed a one-line patch after the move (`/home/bob/RBOX/.venv/bin/python` → `/home/bob/code/RBOX/.venv/bin/python`). If the operator clones fresh somewhere else, `pre-commit install` regenerates the hook for that path automatically.
